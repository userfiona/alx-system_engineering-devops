#!/usr/bin/env bash

# Use read while-loop with null delimiter
find . -maxdepth 1 -type f -name "*-*" ! -name ".*" -print0 | while I read -r -d '' file; do
  name=$(basename "$file" | cut -d '-' -f 2)
  echo "$name"

  grep -H "bash" "$file"
done
